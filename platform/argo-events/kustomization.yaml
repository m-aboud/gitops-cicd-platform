apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argo-events

resources:
  - rbac.yaml
  - event-bus.yaml
  - eventsource-webhook.yaml
  # - eventsource-kafka.yaml   # uncomment if you have Kafka in-cluster
  - sensor-deploy.yaml
  - workflow-template.yaml
